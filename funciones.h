
#include <iostream>
#include <vector>
#include <map>
#include <fstream>
using namespace std;

bool lect_alfabeto(int &, string &, vector<int> &,ifstream &);
bool construye(vector<vector<int> > &,ifstream &,map<string,int> &,vector<bool> &,int &,string &,int &);
bool lect_query(string &,vector<int> &);
